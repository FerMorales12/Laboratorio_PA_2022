#pragma once
ref class Busquedas
{
public:
	//LargoV es la longitud del vector.
	int Secuencial(array<int>^, int search, int largoV);
	int Binario(array<int>^, int search, int largoV);
	bool Burbuja(array<int>^,int largoV);
};

